# RAG

RAG is planned for curated local resources. The foundation keeps raw, custom,
processed, metadata, and cache directories separate and documents resource schema
expectations before indexing or retrieval is implemented.
