# Commands

Phase 0 exposes development CLI commands only: `help`, `classify <action>`, and
`exit`. Assistant command routing is planned for Phase 5 and must pass through
safety policy before external effects.
