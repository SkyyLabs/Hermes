# Testing Strategy

Each implemented phase receives focused automated tests and documentation.
Foundation tests cover settings precedence, runtime path safety, action policy,
and deterministic JSONL record shapes. Later phases should add integration tests
only when real integrations exist.
